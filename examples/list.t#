
# List example 1
push ["T#", "Ruby", "Python", "C", "Go", "Julia", "V", ["HTML", "CSS"]]

push 0
for dup push 7 <= do
    dup -> i drop
    swap -> x
    if push x[i] typeof push list == do
        push x[i][0] print
        push x[i][1] print
    else
        push x[i] print
    end
    swap
    inc
end drop drop


# List example 2
push ["T#", "Ruby", "Python", "C", "Go", "Julia"] dup print

push "V" append dup print

push ["HTML"] append dup print

push "CSS" append[7]

print
